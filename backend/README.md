# YETAOS Backend

FastAPI backend for the YETAOS dev environment orchestrator.

## Quick start

```bash
uv sync --all-groups
uv run uvicorn app.main:app --reload
```

## Tests

```bash
uv run pytest
```